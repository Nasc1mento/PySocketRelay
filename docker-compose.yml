version: '3'
services:
  container1:
    image: python
    build:
      context: simulators/server/
      dockerfile: Dockerfile
    networks:
        - network_simulator
  container2:
    image: python
    build:
      context: simulators/client/
      dockerfile: Dockerfile
    networks:
      - network_simulator
networks:
    network_simulator:
        driver: bridge